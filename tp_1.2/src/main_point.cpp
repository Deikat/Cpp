
#include "Point.hpp"
#include "Cartesien.hpp"
#include "Polaire.hpp"

#include <cstdlib>
#include <vector>
#include <iostream>
#include <cmath>



int main() {
	std::cout<<"atan2: "<<atan2(12,24)<<std::endl;

	/*Cartesien c(10.0,20.0);
	Polaire p(1.0,2.0);	
	
	
	std::vector<Point*> v2;

	v2.push_back(&c);
	v2.push_back(&p);*/
	

	/*std::vector<Point> v1 ;
	v1.push_back(c1);
	v1.push_back(p1);
	v1[0].afficher();
	v1[1].afficher();
	
	for(std::vector<Point * >::iterator it= v2.begin(); it != v2.end(); it++)
		*it->afficher();*/
	
	
	
	return EXIT_SUCCESS;
}
